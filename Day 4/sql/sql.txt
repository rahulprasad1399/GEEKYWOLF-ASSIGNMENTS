-> create table EmployeenNew (EmployeeID INT PRIMARY KEY IDENTITY(1,1), FirstName VARCHAR(50), LastName VARCHAR(50), Department VARCHAR(50), Salary Decimal(10,2), HireDate Date);


// Create a table students and insert names in Malayalam
-> create table students  (studentID INT PRIMARY KEY IDENTITY(1,1), Name NVARCHAR(50));
-> insert into Students (name) values (N'??????'), (N'??????');


// Retrieve all employees who work in Sales, Marketing, or IT departments.
-> select * from EmployeenNew where department='IT' or department='Sales' or department='Marketing';
-> select * from EmployeenNew where department in ('IT','Sales','Marketing');


// Find all employees with salaries ranging from $50,000 to $75,000 (inclusive).
-> select * from EmployeenNew where salary between 50000 and 75000;


// List all employees whose last name begins with the letter 'S'.
-> select * from EmployeenNew where LastName LIKE 'S%';


// Display all employees with exactly five letters in their first name.
-> select * from EmployeenNew where firstname LIKE '____';


// Find employees whose last name starts with either 'B', 'R', or 'S'.
-> select * from EmployeenNew where lastname LIKE '[BRS]%';


// Retrieve all employees whose first name begins with any letter from 'A' through 'M'.
-> select * from EmployeenNew where firstname like '[A-M]%';


// List employees whose last name doesn't start with a vowel (A, E, I, O, U).
-> select * from EmployeenNew where lastname like '[^AEIOU]%';


// Identify employees earning more than $80,000 annually. 
-> select * from EmployeenNew where salary > 80000;


// Find employees who joined the company before 2020. 
-> select * from EmployeenNew where hiredate > '2020-01-01';


// List all employees not named 'John' (first name)
-> select * from EmployeenNew where firstname != 'john';


// Identify Marketing department employees earning $60,000 or less who were hired after June 30, 2019.
-> select * from EmployeenNew where department='Marketing' and salary <= 60000 and hiredate > '2019-06-30';


// Find employees whose first name contains the letters 'an' anywhere and ends with 'e'.
-> select * from EmployeenNew where firstname like '%an%e';